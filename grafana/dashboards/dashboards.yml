apiVersion: 1

providers:
  - name: 'Trading Signals'
    orgId: 1
    folder: 'Trading'
    folderUid: 'trading-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
